import 'package:flutter/material.dart';

class AddMemory extends StatelessWidget {
  const AddMemory({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Dodaj nowe"),
    );
  }
}
